import { BeforeCommaPipe } from './before-comma.pipe';

describe('BeforeCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
